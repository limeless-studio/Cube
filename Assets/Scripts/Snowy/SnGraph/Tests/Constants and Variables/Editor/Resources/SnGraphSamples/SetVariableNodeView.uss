

.nodeView #title {
    background-color: var(--sngraph-node-title-background-color);
    justify-content: center;
    margin: 0;
}

/* TODO: This forced nesting sucks. Can I fix this in the base USS? */

.nodeView #contents > #top > #input,
.nodeView #contents > #top > #output {
    background-color: var(--sngraph-node-title-background-color);
}
